#include <iostream>  // Include the iostream library for input/output

// Instructions to get started with C++ on macOS using VSCode:
// 1. **Install Visual Studio Code**:
//    - Download and install [Visual Studio Code](https://code.visualstudio.com/).

// 2. **Install Xcode Command Line Tools**:
//    - Open Xcode from the App Store, and then install the command line tools by running `xcode-select --install` in the Terminal.

// 3. **Install the C++ Extension for VSCode**:
//    - Open VSCode, go to the Extensions view by clicking the square icon on the sidebar, search for "C++", and install the C++ extension by Microsoft.

// 4. **Write and Run Your Program**:
//    - Use the following C++ code to print "Hello, World!" to the console:
//      ```cpp
//      #include <iostream>  // Include the iostream library for input/output
//      
//      int main() {          // Main function where execution begins
//          std::cout << "Hello, World!" << std::endl;  // Print Hello, World! to the console
//          return 0;         // Return 0 to indicate successful execution
//      }
//      ```

//    - To compile and run your program:
//      - Open VSCode, and use the integrated terminal to navigate to your project directory.
//      - Compile the program with `g++ -o hello hello.cpp` or `clang++ -o hello hello.cpp`.
//      - Execute it with `./hello` in the integrated terminal.

// 5. **Commenting**:
//    - Use `//` for single-line comments and `/* ... */` for multi-line comments to document your code effectively.

int main() {          // Main function where execution begins
    std::cout << "Hello, World!" << std::endl;  // Print Hello, World! to the console
    return 0;         // Return 0 to indicate successful execution
}
